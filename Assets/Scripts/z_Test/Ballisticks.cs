namespace z_Test
{
    public class Ballisticks
    {
        /*[Range(0.01f, 100)] [SerializeField] private float _maxBallSpeed = 0.01f;
        [SerializeField] private float _speedIncreaseStep = 0.01f;*/
        
        /*float potentialEnergy = _maxBallSpeed * _maxBallSpeed + Vector3.Dot(toTarget, Physics.gravity);
          float discriminant = potentialEnergy * potentialEnergy - gSquared * toTarget.sqrMagnitude;


          if (discriminant < 0)
          {
              _maxBallSpeed += _speedIncreaseStep;
              return Vector3.zero;
          }*/

        /*float discRoot = Mathf.Sqrt(discriminant);
        float T_max = Mathf.Sqrt((b + discRoot) * 2f / gSquared);
        float T_min = Mathf.Sqrt((b - discRoot) * 2f / gSquared);
        float T_lowEnergy = Mathf.Sqrt(Mathf.Sqrt(toTarget.sqrMagnitude * 4f / gSquared));*/
    }
}